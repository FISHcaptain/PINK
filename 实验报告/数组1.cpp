//已有一个已正序排好的9个元素的数组，今输入一个数要求按原来排序的规律将它插入数组中。
#include<stdio.h>
int main()
{
	int a[10], i,t=0;
	for (i = 0; i < 9; i++) {
		scanf_s("%d", &a[i]);
	}
	int x = 0;
	scanf_s("%d", &x);
	for (i = 0; i < 9; i++) {
		if (x <= a[i]) {
			t = a[i];
			 a[i]=x;
			x=t;
		}//这一步是想遍历数组然后找出数组和x中最大的值储存
		for (i = 0; i < 10; i++) {
			a[9] = x;//将最大的值赋值给a[9]
			printf("%d\n", a[i]);
		}
	}
 
	return 0;//这个程序并不能解决问题，每次输出x都排在最后一位，但是我实在找不出问题。
}